#include "qtoutputcontroller.h"

/*
GearwheelOutputController::GearwheelOutputController(GearwheelOutputView* view, ProfilMathematisch* zahnrad)
    : view(view), gearwheel(zahnrad)
{
    rotating = false;
    scene = new GearwheelOutputScene(view);
    outputobj = new GearwheelOutputQt(gearwheel->zahnprofil);
    gearwheelitem = new GearwheelItem(*outputobj);
    view->setScene(scene);
    scene->addItem(gearwheelitem);
    view->adjustSize();
}

GearwheelOutputController::~GearwheelOutputController(void)
{
    delete scene;
    delete outputobj;
    delete gearwheelitem;
}
*/
